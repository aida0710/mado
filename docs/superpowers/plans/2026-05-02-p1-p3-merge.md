# P1+P3 Merge 実装プラン

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** front/back を分離 + API を internal/external に 2 プロセス分割 + dev/prod を Docker compose で運用

**Architecture:** Hono を `internal.ts` / `external.ts` の 2 entry に分割。ルートハンドラは相対パスを使い、entry 側で `app.route('/api/internal', subapp)` / `app.route('/api/external', subapp)` で prefix を付ける。Vite proxy (dev) と nginx (prod) が `/api/internal/*` と `/api/external/*` を upstream に振り分ける。

**Tech Stack:** TypeScript, Hono, React + Vite, PostgreSQL 16, Docker compose, nginx

**Spec:** `docs/superpowers/specs/2026-05-02-p1-p3-merge-design.md`

---

## 全体方針メモ

- **TDD**: ルート refactor 系は既存テストが受け入れテストになる。テストの URL を新パスに合わせて先に変更し、production code の path を変えて pass させる。
- **コミット粒度**: 1 タスク 1 コミット原則。Phase A は機能行動に変化がない refactor、Phase B は新規 Docker、Phase C は最終クリーンアップ。
- **workspaces 解体タイミング**: Phase A/B 中は root `package.json` + workspaces を維持 (`npm test --workspaces` が使える状態を保つ)。Phase C で解体する。
- **テスト DB**: 既存テストは `DATABASE_URL_RW_TEST` が無ければ `postgres://dashboard_rw:CHANGEME@localhost:5432/dashboard_test` を使う。テスト用 DB は `docker compose -f compose.dev.yaml up postgres` で立てた postgres コンテナの 5432 ポート (`127.0.0.1` 公開) を使う想定。

---

## Phase A: コード refactor

### Task 1: ルートハンドラの絶対パスを相対パスに置換

**狙い**: 全ルートハンドラ (`/api/...`, `/sql/...`) を相対パス化する。entry 側で `app.route('/api/internal', subapp)` を呼ぶことで最終 URL を構成できるようにする。

**Files:**
- Modify: `api/routes/connections.ts`
- Modify: `api/routes/metrics.ts`
- Modify: `api/routes/notes.ts`
- Modify: `api/routes/settings.ts`
- Modify: `api/routes/sql.ts`
- Modify: `api/routes/storage-favorites.ts`
- Modify: `api/routes/storage-list.ts`
- Modify: `api/routes/storage-preview.ts`
- Modify: `api/routes/storage-readme.ts`
- Modify: `api/routes/connections.test.ts`
- Modify: `api/routes/metrics.test.ts`
- Modify: `api/routes/notes.test.ts`
- Modify: `api/routes/settings.test.ts`
- Modify: `api/routes/sql.test.ts`
- Modify: `api/routes/storage-favorites.test.ts`
- Modify: `api/routes/storage-preview.test.ts`
- Modify: `api/routes/storage-readme.test.ts`

**置換ルール (一括適用)**:
- `/api/connections` → `/connections`
- `/api/connections/:id` → `/connections/:id`
- `/api/metrics` → `/metrics`
- `/api/metrics/push` → `/metrics/push`
- `/api/notes/:slug` → `/notes/:slug`
- `/api/settings/flags` → `/settings/flags`
- `/api/settings/flags/:name` → `/settings/flags/:name`
- `/api/storage/:connId/buckets` → `/storage/:connId/buckets`
- `/api/storage/:connId/list` → `/storage/:connId/list`
- `/api/storage/:connId/readme` → `/storage/:connId/readme`
- `/api/storage/:connId/preview/text` → `/storage/:connId/preview/text`
- `/api/storage/:connId/preview/image` → `/storage/:connId/preview/image`
- `/api/storage/:connId/preview/audio` → `/storage/:connId/preview/audio`
- `/api/storage/:connId/preview/tar` → `/storage/:connId/preview/tar`
- `/api/storage/:connId/preview/tar-entry` → `/storage/:connId/preview/tar-entry`
- `/api/storage/:connId/favorites` → `/storage/:connId/favorites`
- `/api/storage/:connId/favorites/:bucket` → `/storage/:connId/favorites/:bucket`
- `/sql/write` → `/sql/write` (既に相対、変更なし)

→ 規則は **「ルートハンドラの最初の `/api` を削る」**。`/sql/write` は元々 `/api` 始まりではないのでそのまま。

- [ ] **Step 1: テストファイルから先に絶対パスを相対化**

各 `*.test.ts` の `app.request('/api/...', ...)` と `app.request('/sql/write', ...)` を上記置換ルールで一括書き換え。`/sql/write` は無変更。

例 (`api/routes/metrics.test.ts:23`):
```diff
-    const res = await app.request('/api/metrics/push?host=example&command=uptime', {
+    const res = await app.request('/metrics/push?host=example&command=uptime', {
```

ファイル全体に同じ置換を適用。

- [ ] **Step 2: テスト実行 (失敗を確認)**

```bash
cd api
npm test
```
Expected: 多くのテストが 404 になる (実装は絶対パスのまま、テストは相対パスを叩いている → mismatch)。

- [ ] **Step 3: ルートハンドラ側を相対パス化**

`api/routes/*.ts` を上記ルールで一括置換。例 (`api/routes/connections.ts:60-148`):
```diff
-  app.get('/api/connections', async c => {
+  app.get('/connections', async c => {
```
9 ファイル全部に同じ操作を適用。

- [ ] **Step 4: テスト再実行 (pass を確認)**

```bash
cd api
npm test
```
Expected: 全テスト PASS。失敗するなら置換漏れ。

- [ ] **Step 5: Commit**

```bash
git add api/routes/
git commit -m "refactor(api): ルートハンドラの絶対パスを相対化 (entry 側で prefix を付ける方式へ)"
```

---

### Task 2: metrics.ts を Read / Push に分割

**狙い**: `mountMetricsRoutes` を `mountMetricsReadRoutes` と `mountMetricsPushRoutes` に分割。internal/external それぞれの entry が必要な部分だけ mount できるようにする。

**Files:**
- Modify: `api/routes/metrics.ts`
- Modify: `api/routes/metrics.test.ts`
- Modify: `api/index.ts` (一時的、Task 3/4 で削除されるが、それまで動く状態を維持)

- [ ] **Step 1: metrics.test.ts を 2 グループに分割**

`describe('POST /metrics/push', ...)` のブロックは `mountMetricsPushRoutes` で mount したアプリで動かす。`describe('GET /metrics', ...)` (もしくは read 系の describe) は `mountMetricsReadRoutes` で mount したアプリで動かす。

```typescript
// api/routes/metrics.test.ts (構造のみ抜粋)
import { mountMetricsReadRoutes, mountMetricsPushRoutes } from './metrics.js'

const pushApp = new Hono()
mountMetricsPushRoutes(pushApp, { pools, writeToken: 'TKN' })

const readApp = new Hono()
mountMetricsReadRoutes(readApp, { pools })

describe('POST /metrics/push', () => {
  it('inserts a row...', async () => {
    const res = await pushApp.request('/metrics/push?host=example&command=uptime', { ... })
    // ...
  })
  // ...
})

describe('GET /metrics', () => {
  it('returns rows...', async () => {
    const res = await readApp.request('/metrics', { ... })
    // ...
  })
})
```

- [ ] **Step 2: テスト実行 (失敗を確認)**

```bash
cd api
npm run test -- metrics
```
Expected: import エラー (`mountMetricsReadRoutes` / `mountMetricsPushRoutes` が未定義) で全テスト失敗。

- [ ] **Step 3: metrics.ts を 2 関数に分割**

```typescript
// api/routes/metrics.ts
import type { Hono, MiddlewareHandler } from 'hono'
import { bodyLimit } from 'hono/body-limit'
import { z } from 'zod'
import type { Pools } from '../db.js'
import { requireWriteToken } from '../auth.js'
import { MetricsResponseSchema } from '../shared/types.js'
import { isFlagEnabled } from './settings.js'

const MetricRowFromDb = z.object({
  host: z.string(),
  command: z.string(),
  category: z.string(),
  output: z.string(),
  collected_at: z.date(),
})

const FRESHNESS_INTERVAL = '1 hour'
const PUSH_BODY_LIMIT = 1_000_000

function metricsEnabledMiddleware(pools: Pools): MiddlewareHandler {
  return async (c, next) => {
    if (!(await isFlagEnabled(pools, 'metrics'))) {
      return c.json(
        { error: 'metrics is disabled. Please enable it in settings.' },
        503,
      )
    }
    await next()
  }
}

export interface MetricsReadDeps {
  pools: Pools
}

export function mountMetricsReadRoutes(app: Hono, deps: MetricsReadDeps): void {
  const requireMetricsEnabled = metricsEnabledMiddleware(deps.pools)
  app.get('/metrics', requireMetricsEnabled, async c => {
    const r = await deps.pools.ro.query(
      `SELECT host, command, category, output, collected_at
         FROM metrics_latest
         WHERE collected_at >= now() - interval '${FRESHNESS_INTERVAL}'
         ORDER BY category, host, command`
    )
    const rows = r.rows.map(raw => {
      const row = MetricRowFromDb.parse(raw)
      return { ...row, collected_at: row.collected_at.toISOString() }
    })
    return c.json(MetricsResponseSchema.parse(rows))
  })
}

export interface MetricsPushDeps {
  pools: Pools
  writeToken: string
}

export function mountMetricsPushRoutes(app: Hono, deps: MetricsPushDeps): void {
  const requireMetricsEnabled = metricsEnabledMiddleware(deps.pools)
  app.post(
    '/metrics/push',
    requireMetricsEnabled,
    requireWriteToken(deps.writeToken),
    bodyLimit({
      maxSize: PUSH_BODY_LIMIT,
      onError: c => c.json({ error: 'request body too large' }, 413),
    }),
    async c => {
      const host = c.req.query('host')
      const command = c.req.query('command')
      const category = c.req.query('category') ?? 'general'
      if (!host || !command) {
        return c.json({ error: 'host and command query params are required' }, 400)
      }
      const output = await c.req.text()
      await deps.pools.rw.query(
        `INSERT INTO metrics(host, command, category, output)
         VALUES ($1, $2, $3, $4)`,
        [host, command, category, output]
      )
      return c.json({ ok: true })
    },
  )
}
```

旧 `mountMetricsRoutes` と `MetricsDeps` は削除。

- [ ] **Step 4: api/index.ts の呼び出し箇所を更新**

```diff
-import { mountMetricsRoutes } from './routes/metrics.js'
+import { mountMetricsReadRoutes, mountMetricsPushRoutes } from './routes/metrics.js'
```
```diff
-mountMetricsRoutes(app, { pools, writeToken: env.WRITE_TOKEN })
+mountMetricsReadRoutes(app, { pools })
+mountMetricsPushRoutes(app, { pools, writeToken: env.WRITE_TOKEN })
```

- [ ] **Step 5: テスト実行 (pass を確認)**

```bash
cd api
npm test
```
Expected: 全テスト PASS。

- [ ] **Step 6: Commit**

```bash
git add api/routes/metrics.ts api/routes/metrics.test.ts api/index.ts
git commit -m "refactor(api): metrics ルートを Read/Push に分割 (mountMetricsReadRoutes / mountMetricsPushRoutes)"
```

---

### Task 3: api/internal.ts (internal app の entry) を作成

**狙い**: `/api/internal/*` を提供する Hono プロセスの entry を作る。`api/index.ts` はまだ動く状態を保つ (Task 15 で削除)。

**Files:**
- Create: `api/internal.ts`

- [ ] **Step 1: internal.ts を作成**

```typescript
// api/internal.ts
import { serve } from '@hono/node-server'
import { Hono } from 'hono'
import { logger } from 'hono/logger'
import { loadEnv } from './env.js'
import { createPools, closePools } from './db.js'
import { createCrypto } from './crypto.js'
import { createStorageFactory } from './storage.js'
import { mountMetricsReadRoutes } from './routes/metrics.js'
import { mountSqlRoutes } from './routes/sql.js'
import { mountStorageListRoutes } from './routes/storage-list.js'
import { mountStorageReadmeRoutes } from './routes/storage-readme.js'
import { mountStoragePreviewRoutes } from './routes/storage-preview.js'
import { mountStorageFavoritesRoutes } from './routes/storage-favorites.js'
import { mountConnectionsRoutes } from './routes/connections.js'
import { mountNotesRoutes } from './routes/notes.js'
import { mountSettingsRoutes } from './routes/settings.js'

const env = loadEnv()
const pools = createPools({ rw: env.DATABASE_URL_RW, ro: env.DATABASE_URL_RO })
const crypto = createCrypto(env.ENCRYPTION_KEY)
const storageFactory = createStorageFactory({ pools, crypto })

const app = new Hono()
app.use('*', logger())
app.get('/healthz', c => c.text('ok'))

const api = new Hono()
mountMetricsReadRoutes(api, { pools })
mountSqlRoutes(api, { pools, writeToken: env.WRITE_TOKEN })
mountConnectionsRoutes(api, {
  pools,
  crypto,
  invalidate: storageFactory.invalidate,
})
mountStorageListRoutes(api, { getStorage: storageFactory.getStorage })
mountStorageReadmeRoutes(api, { getStorage: storageFactory.getStorage, pools })
mountStoragePreviewRoutes(api, { getStorage: storageFactory.getStorage, env })
mountStorageFavoritesRoutes(api, { pools })
mountNotesRoutes(api, { pools })
mountSettingsRoutes(api, { pools })

app.route('/api/internal', api)

const server = serve({ fetch: app.fetch, port: env.PORT }, info => {
  console.log(`internal listening on http://localhost:${info.port}`)
})

let shuttingDown = false
const shutdown = async () => {
  if (shuttingDown) return
  shuttingDown = true
  setTimeout(() => process.exit(1), 10_000).unref()
  await new Promise<void>(resolve => server.close(() => resolve()))
  await storageFactory.close()
  await closePools(pools)
  process.exit(0)
}
process.on('SIGINT', shutdown)
process.on('SIGTERM', shutdown)

export { app, pools, storageFactory }
```

- [ ] **Step 2: 起動確認 (manual)**

```bash
cd api
PORT=3000 tsx --env-file=../.env internal.ts &
sleep 2
curl -sS http://localhost:3000/healthz
curl -sS http://localhost:3000/api/internal/settings/flags
kill %1
```
Expected:
- `/healthz` が `ok` を返す
- `/api/internal/settings/flags` が JSON を返す (DB 接続成功)
- 失敗するなら `.env` が読めてないか DB が立ってない可能性

- [ ] **Step 3: Commit**

```bash
git add api/internal.ts
git commit -m "feat(api): internal.ts entry を追加 (browser 向け API を /api/internal/* に mount)"
```

---

### Task 4: api/external.ts (external app の entry) を作成

**狙い**: `/api/external/metrics/push` のみを提供する Hono プロセスの entry を作る。

**Files:**
- Create: `api/external.ts`

- [ ] **Step 1: external.ts を作成**

```typescript
// api/external.ts
import { serve } from '@hono/node-server'
import { Hono } from 'hono'
import { logger } from 'hono/logger'
import { loadEnv } from './env.js'
import { createPools, closePools } from './db.js'
import { mountMetricsPushRoutes } from './routes/metrics.js'

const env = loadEnv()
const pools = createPools({ rw: env.DATABASE_URL_RW, ro: env.DATABASE_URL_RO })

const app = new Hono()
app.use('*', logger())
app.get('/healthz', c => c.text('ok'))

const api = new Hono()
mountMetricsPushRoutes(api, { pools, writeToken: env.WRITE_TOKEN })
app.route('/api/external', api)

const server = serve({ fetch: app.fetch, port: env.PORT }, info => {
  console.log(`external listening on http://localhost:${info.port}`)
})

let shuttingDown = false
const shutdown = async () => {
  if (shuttingDown) return
  shuttingDown = true
  setTimeout(() => process.exit(1), 10_000).unref()
  await new Promise<void>(resolve => server.close(() => resolve()))
  await closePools(pools)
  process.exit(0)
}
process.on('SIGINT', shutdown)
process.on('SIGTERM', shutdown)

export { app, pools }
```

- [ ] **Step 2: 起動確認 (manual)**

```bash
cd api
PORT=3001 tsx --env-file=../.env external.ts &
sleep 2
curl -sS http://localhost:3001/healthz
# /api/external/metrics/push を token なしで叩いて 401 を確認
curl -sS -X POST -o /dev/null -w "%{http_code}\n" \
  "http://localhost:3001/api/external/metrics/push?host=h&command=c"
kill %1
```
Expected: `ok` と `401` が返る (`401` は requireWriteToken による拒否、これは正常)。

- [ ] **Step 3: Commit**

```bash
git add api/external.ts
git commit -m "feat(api): external.ts entry を追加 (POST /api/external/metrics/push のみ)"
```

---

### Task 5: front client.ts に API_BASE 定数を導入し全 fetch を /api/internal/* に

**狙い**: `front/lib/api/client.ts` 内の全 fetch URL を `${API_BASE}/...` 形式に書き換える。`API_BASE = '/api/internal'`。`PreviewText.tsx` 等は `client.ts` 経由で URL を取得しているので追加変更不要。

**Files:**
- Modify: `front/lib/api/client.ts`

- [ ] **Step 1: API_BASE 定数を追加し全 URL を書き換え**

`front/lib/api/client.ts` の冒頭 (import 群の後) に:
```typescript
const API_BASE = '/api/internal'
```

そして以下の置換を**ファイル全体に対して**適用 (絶対パスのみ、template literal 含む):
```
/api/metrics                            → ${API_BASE}/metrics
/api/notes/${...}                       → ${API_BASE}/notes/${...}
/api/settings/flags                     → ${API_BASE}/settings/flags
/api/settings/flags/${...}              → ${API_BASE}/settings/flags/${...}
/api/connections                        → ${API_BASE}/connections
/api/connections/${...}                 → ${API_BASE}/connections/${...}
/api/storage/${...}/buckets             → ${API_BASE}/storage/${...}/buckets
/api/storage/${...}/list                → ${API_BASE}/storage/${...}/list
/api/storage/${...}/readme              → ${API_BASE}/storage/${...}/readme
/api/storage/${...}/preview/text        → ${API_BASE}/storage/${...}/preview/text
/api/storage/${...}/preview/image       → ${API_BASE}/storage/${...}/preview/image
/api/storage/${...}/preview/audio       → ${API_BASE}/storage/${...}/preview/audio
/api/storage/${...}/preview/tar         → ${API_BASE}/storage/${...}/preview/tar
/api/storage/${...}/preview/tar-entry   → ${API_BASE}/storage/${...}/preview/tar-entry
/api/storage/${...}/favorites           → ${API_BASE}/storage/${...}/favorites
/api/storage/${...}/favorites/${...}    → ${API_BASE}/storage/${...}/favorites/${...}
```

template literal の例:
```diff
-  metrics: () => getJson('/api/metrics', Metrics),
+  metrics: () => getJson(`${API_BASE}/metrics`, Metrics),
```

- [ ] **Step 2: front の型チェック / build を実行**

```bash
cd front
npm run build
```
Expected: 成功 (型エラーなし、build artifact が生成される)。失敗するなら置換漏れか template literal の構文エラー。

- [ ] **Step 3: front テスト実行**

```bash
cd front
npm test
```
Expected: PASS (テストがあれば)、または「No test files found」。

- [ ] **Step 4: Commit**

```bash
git add front/lib/api/client.ts
git commit -m "refactor(front): API_BASE='/api/internal' を導入し全 fetch URL を新 prefix に"
```

---

## Phase B: Docker infrastructure

### Task 6: api/Dockerfile を作成

**狙い**: api コンテナで `tsx watch` (dev) と `node dist/...` (prod) のどちらでも動かせる Docker image を作る。`lzma-native` のため Debian slim ベースで build deps を入れる。

**Files:**
- Create: `api/Dockerfile`
- Create: `api/.dockerignore`

- [ ] **Step 1: Dockerfile を作成**

```dockerfile
# api/Dockerfile
FROM node:22-bookworm-slim

WORKDIR /app

# lzma-native はネイティブビルドが必要 (python3 + make + g++)
RUN apt-get update \
 && apt-get install -y --no-install-recommends python3 make g++ \
 && rm -rf /var/lib/apt/lists/*

COPY package.json package-lock.json ./
RUN npm ci

COPY . .

EXPOSE 3000 3001

# entry / port は compose 側で上書き
CMD ["npx", "tsx", "internal.ts"]
```

- [ ] **Step 2: .dockerignore を作成**

```
# api/.dockerignore
node_modules
dist
*.log
.git
.env
```

- [ ] **Step 3: build 確認**

```bash
docker build -t web-dashboard-api:dev ./api
```
Expected: 成功。`lzma-native` のビルドエラーが出るなら python3 / make / g++ のいずれかが入ってない。

- [ ] **Step 4: Commit**

```bash
git add api/Dockerfile api/.dockerignore
git commit -m "feat(docker): api/Dockerfile を追加 (node:22-bookworm-slim + build deps for lzma-native)"
```

---

### Task 7: front/Dockerfile を作成 (dev 専用)

**狙い**: `vite dev` をコンテナで `--host 0.0.0.0` で起動する dev 用 image。prod では使わない (prod は nginx multi-stage でビルドする)。

**Files:**
- Create: `front/Dockerfile`
- Create: `front/.dockerignore`

- [ ] **Step 1: Dockerfile を作成**

```dockerfile
# front/Dockerfile (dev 専用、vite dev を 0.0.0.0 で起動)
FROM node:22-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . .

EXPOSE 5173

CMD ["npx", "vite", "dev", "--host", "0.0.0.0", "--port", "5173"]
```

- [ ] **Step 2: .dockerignore を作成**

```
# front/.dockerignore
node_modules
dist
*.log
.git
```

- [ ] **Step 3: build 確認**

```bash
docker build -t web-dashboard-front:dev ./front
```
Expected: 成功。

- [ ] **Step 4: Commit**

```bash
git add front/Dockerfile front/.dockerignore
git commit -m "feat(docker): front/Dockerfile を追加 (dev 専用、vite dev --host 0.0.0.0)"
```

---

### Task 8: vite.config.ts の proxy ターゲットを compose ネット内ホスト名に

**狙い**: Vite dev server が `/api/internal/*` と `/api/external/*` を compose 内部 DNS で解決した api コンテナへ送るように。

**Files:**
- Modify: `front/vite.config.ts`

- [ ] **Step 1: proxy 設定を更新**

```typescript
// front/vite.config.ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [react(), tailwindcss()],
  server: {
    host: true,
    proxy: {
      '/api/internal': 'http://api-internal:3000',
      '/api/external': 'http://api-external:3001',
    },
  },
})
```

(旧 `'/api'` と `'/sql'` のエントリを削除し、新しい 2 エントリに置き換える)

- [ ] **Step 2: Commit**

```bash
git add front/vite.config.ts
git commit -m "refactor(front): vite proxy を compose 内 api-internal:3000 / api-external:3001 へ"
```

---

### Task 9: compose.dev.yaml を作成

**狙い**: dev 用の 4 サービス compose を定義 (postgres + api-internal + api-external + front)。

**Files:**
- Create: `compose.dev.yaml`

- [ ] **Step 1: compose.dev.yaml を作成**

```yaml
# compose.dev.yaml
services:
  postgres:
    image: postgres:16-alpine
    container_name: web-dashboard-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: dashboard
      DASHBOARD_PASSWORD: CHANGEME
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
      - ./db/init:/docker-entrypoint-initdb.d:ro
      - ./db/migrations:/migrations:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d dashboard"]
      interval: 5s
      timeout: 3s
      retries: 10

  api-internal:
    build: ./api
    container_name: web-dashboard-api-internal
    command: ["npx", "tsx", "watch", "internal.ts"]
    env_file:
      - .env
    environment:
      PORT: 3000
    volumes:
      - ./api:/app
      - /app/node_modules
    depends_on:
      postgres:
        condition: service_healthy

  api-external:
    build: ./api
    container_name: web-dashboard-api-external
    command: ["npx", "tsx", "watch", "external.ts"]
    env_file:
      - .env
    environment:
      PORT: 3001
    volumes:
      - ./api:/app
      - /app/node_modules
    depends_on:
      postgres:
        condition: service_healthy

  front:
    build: ./front
    container_name: web-dashboard-front
    ports:
      - "5173:5173"
    volumes:
      - ./front:/app
      - /app/node_modules
    depends_on:
      - api-internal
      - api-external

volumes:
  db_data:
```

- [ ] **Step 2: Commit**

```bash
git add compose.dev.yaml
git commit -m "feat(docker): compose.dev.yaml を追加 (postgres + api-internal + api-external + front)"
```

---

### Task 10: dev 環境の動作確認

**狙い**: `compose.dev.yaml` が期待通り動くことを確認。エンドツーエンドで `/api/internal/...` と `/api/external/metrics/push` が通る。

**Files:** (変更なし、検証のみ)

- [ ] **Step 1: compose 起動**

```bash
# 必要なら DASHBOARD_PASSWORD を変えて .env を準備済みであること
docker compose -f compose.dev.yaml up --build -d
sleep 20  # build と migration 完了まで待機
docker compose -f compose.dev.yaml ps
```
Expected: 4 サービスとも `Up` (postgres は healthy)。

- [ ] **Step 2: front 経由のエンドポイント確認**

```bash
# / が vite dev の HTML を返す
curl -sS http://localhost:5173/ | head -3

# /api/internal/* が api-internal:3000 に proxy される
curl -sS http://localhost:5173/api/internal/settings/flags
```
Expected: HTML と JSON が返る。

- [ ] **Step 3: 直接 api コンテナへの疎通確認 (compose 内ネットワーク経由)**

```bash
# external のヘルスチェック
docker compose -f compose.dev.yaml exec front wget -qO- http://api-external:3001/healthz
docker compose -f compose.dev.yaml exec front wget -qO- http://api-internal:3000/healthz
```
Expected: 両方とも `ok`。

- [ ] **Step 4: HMR 確認 (manual)**

ブラウザで `http://localhost:5173` を開き、`front/App.tsx` か任意の component を 1 行修正して保存、ブラウザに即反映されるか確認。

- [ ] **Step 5: 停止**

```bash
docker compose -f compose.dev.yaml down
```

- [ ] **Step 6: 動作確認結果を記録 (変更なし、Commit なし)**

うまく動かない場合のチェックリスト:
- `.env` がプロジェクトルートにある (`DATABASE_URL_RW`, `DATABASE_URL_RO`, `ENCRYPTION_KEY`, `WRITE_TOKEN` が設定されている)
- DB の URL hostname が `postgres` になっているか (compose 内 DNS、`localhost` ではダメ)
- 例: `DATABASE_URL_RW=postgres://dashboard_rw:CHANGEME@postgres:5432/dashboard`

---

### Task 11: nginx/Dockerfile + nginx/default.conf を作成

**狙い**: prod 用の reverse proxy + 静的配信用 nginx image。multi-stage で front を build → nginx に焼く。

**Files:**
- Create: `nginx/Dockerfile`
- Create: `nginx/default.conf`

- [ ] **Step 1: nginx/default.conf を作成**

```nginx
# nginx/default.conf
server {
    listen 80;
    server_name _;

    # 静的: SPA fallback で client-side ルーティング有効
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }

    location /api/internal/ {
        proxy_pass http://api-internal:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_buffering off;
        proxy_read_timeout 300s;
    }

    location /api/external/ {
        proxy_pass http://api-external:3001;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        client_max_body_size 2m;
    }
}
```

- [ ] **Step 2: nginx/Dockerfile を作成 (multi-stage)**

```dockerfile
# nginx/Dockerfile

# ステージ 1: front を build
FROM node:22-alpine AS frontbuild
WORKDIR /front
COPY front/package.json front/package-lock.json ./
RUN npm ci
COPY front .
RUN npm run build

# ステージ 2: nginx に静的成果物と設定を焼く
FROM nginx:alpine
COPY --from=frontbuild /front/dist /usr/share/nginx/html
COPY nginx/default.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
```

注: このファイルから見たコンテキストは**プロジェクトルート**から build する想定 (compose の `build.context: .` + `dockerfile: nginx/Dockerfile`)。

- [ ] **Step 3: build 確認**

```bash
docker build -f nginx/Dockerfile -t web-dashboard-nginx:prod .
```
Expected: 成功。

- [ ] **Step 4: Commit**

```bash
git add nginx/Dockerfile nginx/default.conf
git commit -m "feat(docker): nginx multi-stage build (front 静的配信 + /api/internal/* /api/external/* リバプロ)"
```

---

### Task 12: compose.prod.yaml を作成

**狙い**: prod 用の 4 サービス compose (postgres + api-internal + api-external + nginx)。dev と違って api は `tsx watch` ではなく `tsc + node dist/...` を使う。

**Files:**
- Create: `compose.prod.yaml`
- Modify: `api/package.json` (`build` script、`start:internal`、`start:external` を追加)
- Modify: `api/tsconfig.json` (必要なら、`outDir: ./dist` であることを確認)

- [ ] **Step 1: api/package.json の scripts を整備**

```jsonc
{
  "name": "@web-dashboard/api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev:internal": "tsx watch --env-file=../.env internal.ts",
    "dev:external": "tsx watch --env-file=../.env external.ts",
    "build": "tsc -p tsconfig.json",
    "start:internal": "node dist/internal.js",
    "start:external": "node dist/external.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  // dependencies / devDependencies は既存通り
}
```

(旧 `dev` と `start` を削除して上記に置き換える)

- [ ] **Step 2: tsconfig.json で `outDir` を確認**

`api/tsconfig.json` を Read し `compilerOptions.outDir` が `./dist` (もしくは `dist`) になっていることを確認。なってなければ追加。

- [ ] **Step 3: compose.prod.yaml を作成**

```yaml
# compose.prod.yaml
services:
  postgres:
    image: postgres:16-alpine
    container_name: web-dashboard-postgres-prod
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: dashboard
      DASHBOARD_PASSWORD: CHANGEME
    volumes:
      - db_data_prod:/var/lib/postgresql/data
      - ./db/init:/docker-entrypoint-initdb.d:ro
      - ./db/migrations:/migrations:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d dashboard"]
      interval: 5s
      timeout: 3s
      retries: 10

  api-internal:
    build: ./api
    container_name: web-dashboard-api-internal-prod
    command: ["sh", "-c", "npm run build && node dist/internal.js"]
    env_file:
      - .env
    environment:
      PORT: 3000
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  api-external:
    build: ./api
    container_name: web-dashboard-api-external-prod
    command: ["sh", "-c", "npm run build && node dist/external.js"]
    env_file:
      - .env
    environment:
      PORT: 3001
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  nginx:
    build:
      context: .
      dockerfile: nginx/Dockerfile
    container_name: web-dashboard-nginx-prod
    ports:
      - "80:80"
    depends_on:
      - api-internal
      - api-external
    restart: unless-stopped

volumes:
  db_data_prod:
```

注:
- prod では postgres ポートは host に公開しない (compose 内部ネットワークのみ)
- api コンテナはホストにポート公開しない (nginx 経由のみ)
- nginx の `:80` のみ host に公開
- api の `command: sh -c "npm run build && node ..."` は image 起動時に毎回 build。production で複数回起動するなら multi-stage api/Dockerfile に build ステージを追加するほうが効率的だが、本 spec のスコープでは「build → start を 1 コマンドで」が最小実装

- [ ] **Step 4: Commit**

```bash
git add compose.prod.yaml api/package.json api/tsconfig.json
git commit -m "feat(docker): compose.prod.yaml + api scripts 整備 (postgres + api-internal + api-external + nginx)"
```

---

### Task 13: prod 環境の動作確認

**狙い**: `compose.prod.yaml` が期待通り動くことを確認。

**Files:** (変更なし、検証のみ)

- [ ] **Step 1: 起動**

```bash
docker compose -f compose.prod.yaml up --build -d
sleep 30  # build + migration + tsc 完了まで待機
docker compose -f compose.prod.yaml ps
```
Expected: 4 サービス全て `Up`。

- [ ] **Step 2: nginx 経由のエンドポイント確認**

```bash
# / は SPA HTML
curl -sS http://localhost:80/ | head -3

# /api/internal/* は api-internal へ
curl -sS http://localhost:80/api/internal/settings/flags

# /api/external/metrics/push は token なしで 401 (内容到達確認)
curl -sS -X POST -o /dev/null -w "%{http_code}\n" \
  "http://localhost:80/api/external/metrics/push?host=h&command=c"
```
Expected: HTML, JSON, `401` (ルーティング正常)。

- [ ] **Step 3: 停止**

```bash
docker compose -f compose.prod.yaml down
```

- [ ] **Step 4: (Commit なし、検証のみ)**

うまく動かない場合のチェックリスト:
- `.env` の `DATABASE_URL_RW` `DATABASE_URL_RO` が `postgres` ホスト名を指しているか
- nginx のログで `proxy_pass` 先が解決できてるか (`docker compose -f compose.prod.yaml logs nginx`)
- api-internal / api-external のログで起動失敗していないか

---

## Phase C: クリーンアップ

### Task 14: 外部 cron スクリプトの URL を `/api/external/metrics/push` へ更新

**狙い**: 外部スパコンに置くスクリプトのソースを新 URL に。

**Files:**
- Modify: `metrics/db.py`
- Modify: `api/cron-samples/push.sh`
- Modify: `api/cron-samples/README.md` (もし URL 例を載せていれば)
- Modify: `metrics/README.md` (もし URL 例を載せていれば)

- [ ] **Step 1: metrics/db.py を更新**

```diff
-    url = f"{base.rstrip('/')}/api/metrics/push?{qs}"
+    url = f"{base.rstrip('/')}/api/external/metrics/push?{qs}"
```

docstring も更新:
```diff
-    """POST `output` to /api/metrics/push as text/plain.
+    """POST `output` to /api/external/metrics/push as text/plain.
```

- [ ] **Step 2: api/cron-samples/push.sh を更新**

```diff
-  "$DASHBOARD_URL/api/metrics/push?host=$HOST&command=$COMMAND"
+  "$DASHBOARD_URL/api/external/metrics/push?host=$HOST&command=$COMMAND"
```

- [ ] **Step 3: README 群を grep して URL を確認**

```bash
grep -rn "/api/metrics/push" /Users/aida/PhpstormProjects/web-dashboard/metrics /Users/aida/PhpstormProjects/web-dashboard/api/cron-samples /Users/aida/PhpstormProjects/web-dashboard/README.md /Users/aida/PhpstormProjects/web-dashboard/docs 2>/dev/null
```
ヒットした箇所を `/api/external/metrics/push` に置換。docs/superpowers/specs 配下は本 spec / roadmap でのみ歴史的記述として残るので、更新せず。

- [ ] **Step 4: Commit**

```bash
git add metrics/ api/cron-samples/
git commit -m "refactor(metrics): push エンドポイントを /api/external/metrics/push に更新 (P1+P3 統合)"
```

---

### Task 15: workspaces 解体 (root package.json 削除 + eslint 分散)

**狙い**: ルート `package.json`、`package-lock.json`、`eslint.config.js` を削除。eslint 設定は `front/` と `api/` それぞれに移植。

**Files:**
- Delete: `package.json` (root)
- Delete: `package-lock.json` (root)
- Delete: `eslint.config.js` (root)
- Create: `front/eslint.config.js`
- Create: `api/eslint.config.js`
- Modify: `front/package.json` (eslint 関連 devDeps を追加 + lint script)
- Modify: `api/package.json` (eslint 関連 devDeps を追加 + lint script)

- [ ] **Step 1: front/eslint.config.js を作成 (React 用)**

```javascript
// front/eslint.config.js
import js from '@eslint/js'
import globals from 'globals'
import reactHooks from 'eslint-plugin-react-hooks'
import reactRefresh from 'eslint-plugin-react-refresh'
import tseslint from 'typescript-eslint'
import { defineConfig, globalIgnores } from 'eslint/config'

export default defineConfig([
  globalIgnores(['dist']),
  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      js.configs.recommended,
      tseslint.configs.recommended,
      reactHooks.configs.flat.recommended,
      reactRefresh.configs.vite,
    ],
    languageOptions: {
      globals: globals.browser,
    },
  },
])
```

- [ ] **Step 2: api/eslint.config.js を作成 (Node 用、React なし)**

```javascript
// api/eslint.config.js
import js from '@eslint/js'
import globals from 'globals'
import tseslint from 'typescript-eslint'
import { defineConfig, globalIgnores } from 'eslint/config'

export default defineConfig([
  globalIgnores(['dist']),
  {
    files: ['**/*.ts'],
    extends: [
      js.configs.recommended,
      tseslint.configs.recommended,
    ],
    languageOptions: {
      globals: globals.node,
    },
  },
])
```

- [ ] **Step 3: front/package.json に eslint 関連 devDeps と lint script を追加**

`front/package.json` の devDependencies に:
```diff
   "devDependencies": {
+    "@eslint/js": "^10.0.1",
+    "eslint": "^10.2.1",
+    "eslint-plugin-react-hooks": "^7.1.1",
+    "eslint-plugin-react-refresh": "^0.5.2",
+    "globals": "^17.5.0",
+    "typescript-eslint": "^8.58.2",
     "@tailwindcss/typography": "^0.5.19",
     ...
   }
```

scripts に:
```diff
   "scripts": {
     "dev": "vite",
     "build": "tsc -b && vite build",
     "preview": "vite preview",
     "test": "vitest run",
-    "test:watch": "vitest"
+    "test:watch": "vitest",
+    "lint": "eslint ."
   },
```

- [ ] **Step 4: api/package.json に eslint 関連 devDeps と lint script を追加**

devDependencies に:
```diff
   "devDependencies": {
+    "@eslint/js": "^10.0.1",
+    "eslint": "^10.2.1",
+    "globals": "^17.5.0",
+    "typescript-eslint": "^8.58.2",
     "@types/pg": "^8.20.0",
     ...
   }
```

scripts に:
```diff
   "scripts": {
     "dev:internal": "tsx watch --env-file=../.env internal.ts",
     ...
-    "test:watch": "vitest"
+    "test:watch": "vitest",
+    "lint": "eslint ."
   },
```

- [ ] **Step 5: ルートのファイルを削除**

```bash
rm package.json package-lock.json eslint.config.js
```

- [ ] **Step 6: 各サブディレクトリで npm install**

```bash
cd front && npm install
cd ../api && npm install
cd ..
```
Expected: それぞれ成功し新しい `package-lock.json` が生成。

- [ ] **Step 7: lint と test が両サブディレクトリで通ることを確認**

```bash
cd front && npm run lint && npm run build && cd ..
cd api && npm run lint && npm run test && cd ..
```
Expected: 全て成功。

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "refactor: workspaces 解体 (root package.json 削除、eslint 設定を front/ と api/ に分散)"
```

---

### Task 16: 古い `api/index.ts` と `compose.yml` を削除

**狙い**: P1+P3 統合後、不要になった entry と旧 compose を片付ける。

**Files:**
- Delete: `api/index.ts`
- Delete: `compose.yml`

- [ ] **Step 1: 削除**

```bash
rm api/index.ts compose.yml
```

- [ ] **Step 2: api のテストを再実行 (api/index.ts への参照が残ってないことを確認)**

```bash
cd api && npm test && cd ..
```
Expected: PASS。失敗するなら `api/index.ts` を import している箇所がある (テスト・ファイル等)、追跡して修正。

- [ ] **Step 3: dev compose を再起動して動作確認**

```bash
docker compose -f compose.dev.yaml up --build -d
sleep 20
curl -sS http://localhost:5173/api/internal/settings/flags
docker compose -f compose.dev.yaml down
```
Expected: JSON が返る。

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "chore: 古い api/index.ts と compose.yml を削除 (P1+P3 統合で不要)"
```

---

### Task 17: roadmap doc に統合 note を追記

**狙い**: 旧 roadmap (`docs/superpowers/specs/2026-05-01-front-back-separation-roadmap.md`) に「P2 中止 + P1+P3 を本 spec に統合」の履歴を残す。

**Files:**
- Modify: `docs/superpowers/specs/2026-05-01-front-back-separation-roadmap.md`

- [ ] **Step 1: 末尾に追記**

ファイルの末尾 (`次セッションへの引き継ぎ` セクションの後) に以下を追加:

```markdown
---

## 2026-05-02 方針変更

本 roadmap の実施途中で以下を決定:

- **P2 (FastAPI 移行) は中止**。Hono (TypeScript) のまま続行する方針に。
- **P1 と P3 を統合し、`docs/superpowers/specs/2026-05-02-p1-p3-merge-design.md` を新たな統合 spec として起こした**。本 roadmap の P1 / P3 セクションは歴史的記述として残す (上書きはしない)。
- 「次セッションへの引き継ぎ」のフェーズ進行は無効。今後の作業は統合 spec とその plan (`docs/superpowers/plans/2026-05-02-p1-p3-merge.md`) に従う。
```

- [ ] **Step 2: Commit**

```bash
git add docs/superpowers/specs/2026-05-01-front-back-separation-roadmap.md
git commit -m "docs(roadmap): P2 中止と P1+P3 統合 spec への参照を追記"
```

---

## 完了基準

全タスク完了後、以下が成立していること:

1. **API パス**: 旧 `/api/*` `/sql/*` は廃止、新 `/api/internal/*` `/api/external/*` のみ動作
2. **dev**: `docker compose -f compose.dev.yaml up` で 4 サービス (postgres + api-internal + api-external + front) が起動、ブラウザから `http://localhost:5173/` で front アクセス可、`/api/internal/*` の API 呼び出しが Vite proxy 経由で疎通、HMR が効く
3. **prod**: `docker compose -f compose.prod.yaml up -d` で 4 サービス (postgres + api-internal + api-external + nginx) が起動、`http://localhost/` で front アクセス可、`/api/internal/*` `/api/external/*` が nginx 経由で疎通
4. **テスト**: `cd api && npm test` と `cd front && npm run build` がそれぞれ成功
5. **lint**: `cd api && npm run lint` と `cd front && npm run lint` がそれぞれ成功
6. **workspaces 解体**: ルートに `package.json` `package-lock.json` `eslint.config.js` `compose.yml` `api/index.ts` が存在しない
7. **外部 cron**: `metrics/db.py` の URL が `/api/external/metrics/push` を指す
8. **roadmap doc**: 旧 roadmap 末尾に「P2 中止 + P1+P3 統合」の note がある
